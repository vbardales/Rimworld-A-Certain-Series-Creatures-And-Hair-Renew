# Runs, one line each

Newest last. The evidence itself is never kept as folders: only the latest report that still proves something stays
on disk (`Tests/Pickle/Evidence/`, ignored by git). A run that needs more than a line gets a file next to this one.

- 2026-09-24 21:04, pass 1 English, first run of the suite, on `56ef72d` (request 20260924-164530-619-a69c; the dispatcher's worker died mid-run, the game finished alone): `exitReason: watchdog-timeout`, 15 of 20 scenarios played, 8 passed, 7 failed, 5 never played. All 7 failures are one game error, `Tried 300 times to generate age` (both creature kinds set `maxGenerationAge` 0), the mod's own inherited defect, fixed in `a0fa40c`. The watchdog ended the run in the egg scenario (a whole game day does not fit 120 s), fixed in `a354f06`. Neither fix replayed. Superseded build: only `summary.json`, `summary.md`, `junit.xml` and `Player.log` kept in `Tests/Pickle/Evidence/2026-09-24-english/`; the 1.7 GB archive and its captures deleted, none opened.
- 2026-09-24 22:32, pass 2 French (request 20260924-164531-200-7523), on the tree of `a354f06`: `exitReason: failed`, 13 of 13 scenarios played, 11 passed, 1 failed, 1 skipped by requirement (feature 15, no Animal Prosthetics 2). The age fix holds: both creatures spawn, the claws, the wings and the two butcherings pass, and the save round trip with an egg mid-incubation passes. One failure, the suite's: `13-labels-fr` "the labels a player reads", `'ACS_AngelCore' names more than one def (BodyPartDef, BodyPartGroupDef, ThingDef)` (assumption 2); features 12 to 14 rewritten with a typed local step (20 steps), not replayed. Captures opened (health tab claws, wings, research window): French labels and distinct sides read correctly; the research capture shows the left of the tree only, not the mod's project. Minified to 0.7 MB in `Tests/Pickle/Evidence/2026-09-24-french/`.
- 2026-09-25 09:54 (UTC log time), fix ticket `898c`, `02-creatures-en` in English, on the tree of `79d5fad`: `exitReason: passed`, 3 of 3 played and passed, 0 flaky, 1 attempt. The three scenarios that failed on the age error in the first run are green: both creatures draw, the beetle's health tab names its front claws each on its own side ("Front left claw" Cut, "Front right claw" Bruise), the seraph's tells its eight wings apart (First, Third, Fifth, Seventh wing shown) and puts the cut on "Right leg". Ages read sane (beetle 4, seraph 4558). Captures opened, all three. Log: only the three start-up "did not load any content" lines, no game error. Minified to 0.4 MB in `Tests/Pickle/Evidence/2026-09-24-fix-creatures-en/`. A fix ticket, not a certification.
- 2026-09-25 ~10:00 (UTC log time), fix ticket `ca7f`, `08-ranged-attacks` in English, on the tree of `79d5fad`: `exitReason: failed`, 2 of 2 played, both failed, 1 attempt. Both scenarios die the same way: the watch step "fires at (152, 155) and its projectiles are watched" times out after 100 s, and the log shows a `System.NullReferenceException` in `Verse.VerbProperties.GetForceMissFactorFor` on every tick of the firing creature, from `Verb_LaunchProjectile.TryCastShot`. **The mod's own defect, not the suite's:** in this game build (1.6.4871) `TryCastShot` calls that method whenever the verb's `forcedMissRadius` is above 0.5, with `Verb.EquipmentSource` as its first argument, and the method reads `equipment.def` unconditionally; an innate verb has no equipment, so it throws. Both creatures' verbs set `forcedMissRadius` (beetle 5, seraph 10): neither ever fires (read from the game's own IL, with Mono.Cecil). Assumption 6 held in part: the wild creature accepted `TryStartCastOn`. Minified to 2.3 MB in `Tests/Pickle/Evidence/2026-09-24-fix-shots/`.
- 2026-09-25 ~10:26 (UTC log time), fix ticket `1a1e`, `12-labels-en` in English, on the tree of `79d5fad`: `exitReason: passed`, 2 of 2 played and passed, 0 flaky, 1 attempt. The labels of the loaded defs read in English, including the three names shared by several def types (angel core, beetle race and kind, seraph race and kind) through the typed local step, and the activity texts of the propagator. Log: only the three start-up "did not load any content" lines. No capture in this feature. Minified in `Tests/Pickle/Evidence/2026-09-24-fix-labels-en/`.
- 2026-09-25 ~10:28 (UTC log time), fix ticket `22a0`, `13-labels-fr` in French, on the tree of `79d5fad`: `exitReason: passed`, 2 of 2 played and passed, 0 flaky, 1 attempt. The scenario that failed on the ambiguous name in the French pass is green: every French label of the loaded defs reads as written (the ten labels, the typed reads of the angel core, the beetle and the seraph, and the propagator's activity texts). Log: only the three start-up "did not load any content" lines. Minified in `Tests/Pickle/Evidence/2026-09-24-fix-labels-fr/`.
- 2026-09-25, fix tickets `a7cc` and `f8a6`, on the tree of `79d5fad`: `10-egg-hatching` (English, 1 scenario) passed in 54.9 s with the egg started at 95 percent, the hatchling without a faction and no game error; `14-labels-zh` (ChineseSimplified, 1 scenario) passed in 1.6 s, the preserved Chinese labels read as written. Both `exitReason: passed`, 0 flaky, only the three start-up "did not load any content" lines in their logs. Minified in `Tests/Pickle/Evidence/2026-09-24-fix-egg/` and `2026-09-24-fix-labels-zh/`.
- 2026-09-25 12:46 (UTC log time), fix ticket `4f41`, `08-ranged-attacks` in English, on the tree of `735f67c`: `exitReason: passed`, 2 of 2 played and passed (horn 28.2 s, sweep 34.3 s), 0 flaky, 1 attempt. Both creatures' shots leave and land for the first time. Log: seven `[ERROR]` lines, three the usual "did not load any content" and **four `Config error in ACS_DarkMatterBeetle / ACS_Gabriel: verb 0: has incorrect forcedMiss settings`** at start-up, outside the scenarios and so invisible to them: the game wants a `forcedMissRadius` above 0 on an explosive projectile, and 0 (the field dropped) trips it. Fixed by setting 0.5, not replayed. Minified in `Tests/Pickle/Evidence/2026-09-25-fix-shots-2/`.
- 2026-09-25 (afternoon), fix ticket `34d1`, `08-ranged-attacks` in English, on the tree of `0aa961c` (`forcedMissRadius` 0.5): `exitReason: passed`, 2 of 2 played and passed (horn 30.3 s, sweep 40.0 s), 0 flaky, 1 attempt. Log: three `[ERROR]` lines, the usual "did not load any content" ones; no `NullReferenceException`, no `Config error`. Both defects of the shots are gone. Minified in `Tests/Pickle/Evidence/2026-09-25-fix-shots-3/`; the report of `4f41` (`2026-09-25-fix-shots-2`, superseded build) is kept only as the record of the config errors.
- 2026-09-25 17:35 (UTC log time), **complete pass 1 English** (request `3a98`), on the tree of `0aa961c` (Mod and Tests identical at `052be8f`): `exitReason: passed`, **20 of 20 scenarios discovered and played: 19 passed, 0 failed, 1 skipped by requirement** (feature 15, Animal Prosthetics 2 not mounted, by design), 0 flaky, 1 attempt. Log: three `[ERROR]` lines, the usual start-up "did not load any content"; no `NullReferenceException`, no `Config error`. All seven `@review` captures opened. Creatures (beetle and seraph side by side, claws "Front left claw" Cut and "Front right claw" Bruise, eight distinct wing labels and "Right leg", ages 1 and 685) read right. **Two captures do not show what they are for:** the research window (`manual--research-dark-matter-propagation`) shows only the left of the tree, the mod's project at (19, 3) is out of frame, and the three hairstyle captures (misaka, accelerator, index) are wide map views where colonists are a few pixels tall, so the style cannot be told; the scenarios pass on their assertions (the text listing of the tab, `hairDef`), not on these pictures. Minified to 1.1 MB in `Tests/Pickle/Evidence/2026-09-25-complete-english/`; the launcher's archive `0925-1735` (46 MB, mine, no `keep.txt`) deleted.
- 2026-09-25 17:40 (UTC log time), **complete pass 2 French** (request `5ff2`), on the tree of `0aa961c`: `exitReason: passed`, **13 of 13 scenarios discovered and played: 12 passed, 0 failed, 1 skipped by requirement** (feature 15), 0 flaky, 1 attempt. Log: three `[ERROR]` lines (the usual start-up ones), no `NullReferenceException`, no `Config error`. The three `@review` captures opened: the health tab in French ("Griffe avant gauche" Taillade and "Griffe avant droite" Contusion on two lines, "Première, Troisième, Cinquième, Septième aile" and "Jambe droite", "Scarabée rhinocéros blanc", "Puissance de Dieu", ages "1 saison" and 1481) fits its columns, nothing clipped. The research capture again shows only the left of the tree, the mod's project out of frame, in French too. Minified to 0.6 MB in `Tests/Pickle/Evidence/2026-09-25-complete-french/`; the launcher's archive `0925-1740` (17 MB, mine, no `keep.txt`) deleted.
- 2026-09-25 15:41 (UTC log time), **complete pass 3 Chinese** (request `cb37`), tree of `0aa961c`: **no run.** The game crashed at start-up with exit code 139 (`Caught fatal signal - signo:11`), after the mods had loaded and before any scenario; no report was written (`no-report.txt`). The stack in `Player.log` is Mono and Unity (`burst_mono_simulate_burst_debug_domain_reload`, `GC_inner_start_routine`) with no frame of this mod; the same signature as crashes of two other mods the same day (Housebroken 17:04, Ebbbs 16:51), which makes it an intermittent fault of the game under the WSL, cause unknown. Nothing read against the mod. Replayed once, as request `20260925-174340-016-d6ae`, evidence to `2026-09-25-complete-chinese-2` (its result is the next line once it is done); the crash log (28 KB) is kept in `Tests/Pickle/Evidence/2026-09-25-complete-chinese/`.
- Fix tickets of the first round, taken together: `02`, `10`, `12`, `13`, `14` green, `08` red (the shots, `forcedMissRadius`, fixed afterwards and not yet replayed).
- 2026-09-25 22:03 (UTC log time), fix ticket `421e` (features 05 and 06, English, the two reworked captures), tree of `83232d1`: **no run.** The game crashed at start-up, exit code 139 (SIGSEGV), before any scenario; no report (`no-report.txt`). The dispatcher counts it the fourth such crash in 35 minutes on the machine, across other mods (Ancient Chinese Beast, Fieldwork, PickleTools), and the WSL `dmesg` shows `RimWorldLinux` faulting: not this mod. Nothing read against it. Replayed once as `11fd`, evidence `2026-09-25-fix-captures-2`; the crash log (26 KB) stays in `2026-09-25-fix-captures/`.
- 2026-09-25 22:05 (UTC log time), pass 4 request `3af4` (feature 15 only, English, Animal Prosthetics 2 mounted), tree of `83232d1`: `exitReason: failed`, 1 of 1 played, 0 passed, 0.4 s: "ACS_DarkMatterBeetle is offered 26 recipes and ACS_Gabriel 26: expected more for the first". **Cause found in the staging, not in the patch:** `stage-pickle-wsl.sh` activates the mod under test after every mod of the pass map and does not sort by `loadBefore`, so Animal Prosthetics 2 loaded before this mod, copied its category lists into its surgeries first, and the beetle was added too late (the exact case `About.xml` and the patch comment warn about). Fixed in `Tests/Pickle/wsl-deps.avec-ads2.map`, which now names this mod (`path:`) ahead of the other one; the staging then does not add it twice. Replayed as `b7f4`, evidence `2026-09-25-ads2-2`; the report of `3af4` is minified in `2026-09-25-ads2/`. Not yet proved that the order was the whole cause: `b7f4` says.
